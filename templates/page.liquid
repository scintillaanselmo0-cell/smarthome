<div style="max-width:800px;margin:0 auto;padding:80px 48px;">
  <h1 style="font-family:'Syne',sans-serif;font-size:2.5rem;font-weight:800;margin-bottom:32px;">{{ page.title }}</h1>
  <div style="color:var(--muted);line-height:1.8;font-size:0.95rem;">
    {{ page.content }}
  </div>
</div>
